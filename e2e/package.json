{
  "name": "kirgo-e2e",
  "private": true,
  "description": "Playwright + Visual QA suite (Parts B/C). Isolated from the Next app build.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
